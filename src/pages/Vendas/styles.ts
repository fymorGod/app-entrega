import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    infoNotRequests: {
        flex: 1,
        marginTop: 10,
        padding: 10
    },
    infoNotRequestsText: {
        fontSize: 18,
        fontWeight: '500',
        color: '#DC0000',
        textAlign: 'center'
    },
})